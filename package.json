{
  "name": "vietnamcanvas",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "generate": "BUILD_TARGET=static nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate-pwa-assets": "pwa-assets-generator public/logo.svg --preset minimal",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply vietnam-canvas-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply vietnam-canvas-db --remote",
    "db:seed": "wrangler d1 execute vietnam-canvas-db --local --file=./db/seed.sql",
    "db:studio": "TARGET=local drizzle-kit studio",
    "db:studio:remote": "drizzle-kit studio",
    "import:destinations": "npx tsx scripts/import_destinations.ts",
    "create-admin": "npx tsx scripts/create_admin.ts",
    "build:api": "BUILD_TARGET=api nuxt build --dotenv .env.prod",
    "deploy:api": "npx tsx scripts/deploy_api.ts",
    "dev:api": "npx wrangler dev .output/server/index.mjs --config wrangler.worker.json",
    "build:ssg": "npx tsx scripts/build_ssg.ts",
    "deploy:ssg": "wrangler pages deploy dist --env-file .env.prod",
    "deploy:web": "npm run build:ssg && npm run deploy:ssg",
    "deploy": "npm run build:api && npm run deploy:api && npm run build:ssg && npm run deploy:ssg"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^25.0.3",
    "@vueuse/nuxt": "^14.1.0",
    "bcryptjs": "^3.0.3",
    "cheerio": "^1.1.2",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "gsap": "^3.14.2",
    "jose": "^6.1.3",
    "lucide-vue-next": "^0.562.0",
    "nuxt": "^4.2.2",
    "nuxt-security": "^2.5.0",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^10.2.1",
    "@nuxtjs/seo": "^3.3.0",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@vite-pwa/assets-generator": "^1.0.2",
    "drizzle-kit": "^0.31.8",
    "sass": "^1.97.1",
    "wrangler": "^4.54.0"
  }
}
